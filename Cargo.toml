[workspace]
members = [
  # the main library and tests
  "rustls",
  # tests and example code that depend on mio
  "rustls-mio",
]
exclude = ["admin/rustfmt"]
resolver = "2"

[patch.crates-io]
rustls = { path = "./rustls" }
